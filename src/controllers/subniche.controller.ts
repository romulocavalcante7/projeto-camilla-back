import { Request, Response } from "express";

const createSubniche = (req: Request, res: Response) => {
  // Implementação para criar um novo subnicho
};

const getAllSubniches = (req: Request, res: Response) => {
  // Implementação para listar todos os subnichos
};

const getSubnicheById = (req: Request, res: Response) => {
  // Implementação para buscar um subnicho por ID
};

const updateSubniche = (req: Request, res: Response) => {
  // Implementação para atualizar informações de um subnicho
};

const deleteSubniche = (req: Request, res: Response) => {
  // Implementação para deletar um subnicho
};

export default {
  createSubniche,
  getAllSubniches,
  getSubnicheById,
  updateSubniche,
  deleteSubniche,
};
